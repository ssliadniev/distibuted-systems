## 🛠️ Quick Start
This is the recommended way to run the project.

1. Run mongo store (Part I)
    ```bash
    cd cassandra-store
    docker compose up -d --build
    ```

2. Run web counter (Part II)
    ```bash
    cd web-counter
    docker compose up -d --build
    ```
